%main{:role => "main"}
  %section.jumbotron.text-center
    .container.box
      %h1.jumbotron-heading.text-white.font-custum Only one Album in the world
      %p.lead.text-white.font-custum
        Combine your travel memories and momentary memories into one book.
        The memories will always remain with your heart.
        I will always keep those wonderful memories.
      %p
        = link_to 'Create Album', new_picture_path, class: "btn btn-primary my-2 text-white"
        %a.btn.btn-secondary.my-2.text-white{:href => "#"} Show Photos
  .album.bg-dark.py-5.bg-light
    .container
      .row
        - @pictures.each do |picture|
          = render partial: "form", locals: { picture: picture }
%footer.text-muted
  .container
    %p.float-right
      %a{:href => "#"} Back to top
    %p Album example is © Bootstrap, but please download and customize it for yourself!
    %p
      New to Bootstrap?
      %a{:href => "{{ site.url }}/"} Visit the homepage
      or read our
      = succeed "." do
        %a{:href => "{{ site.baseurl }}/docs/{{ site.docs_version }}/getting-started/introduction/"} getting started guide
